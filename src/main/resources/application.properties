
# Application configuration
spring.application.name=DRUPSCBLOGBACKEND

# Server configuration
server.port=8080

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/koffeeclan
spring.datasource.username=root
spring.datasource.password=Avinash@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Devtools configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# OpenAI API Configuration
# openai.api-key=${OPENAI_API_KEY}
# openai.model=gpt-3.5-turbo   # (or gpt-4 if you have access)
# openai.temperature=0.7        # Adjust creativity level
# openai.max-tokens=500         # Limit response size
# openai.top-p=1.0              # Sampling method
# openai.frequency-penalty=0.0  # Avoid repetitive responses
# openai.presence-penalty=0.0   # Encourage diverse responses



